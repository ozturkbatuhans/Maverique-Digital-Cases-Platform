
<div class="section-process-step">
  <div class="container">
    <div class="row row-grid align-items-center justify-content-between">
      {{#if isLeft}}
      <div class="col-xl-5 col-lg-6 order-lg-2">
        <div class="pr-md-4">
          <span class="badge badge-warning badge-pill">{{tag}}</span>
          <h3 class="h2 mt-4">{{title}}</h3>
          <p class="lead lh-190 my-4">{{description}}</p>
        </div>
      </div>
      <div class="col-lg-6">
        <div class="card border-0 shadow-lg">
          <figure>
            <img alt="Image placeholder" src="{{imageUrl}}" class="card-img">
          </figure>
        </div>
      </div>
      {{else}}
      <div class="col-lg-6">
        <div class="card border-0 shadow-lg">
          <figure>
            <img alt="Image placeholder" src="{{imageUrl}}" class="card-img">
          </figure>
        </div>
      </div>
      <div class="col-xl-5 col-lg-6">
        <div class="pr-md-4">
          <span class="badge badge-warning badge-pill">{{tag}}</span>
          <h3 class="h2 mt-4">{{title}}</h3>
          <p class="lead lh-190 my-4">{{description}}</p>
        </div>
